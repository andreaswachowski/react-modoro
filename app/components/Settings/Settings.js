import React from 'react';
import { View, Text } from 'react-native';

Settings.propTypes = {

};

export default function Settings(props) {
  return (
    <View>
      <Text>
        {'Settings'}
      </Text>
    </View>
  );
}
